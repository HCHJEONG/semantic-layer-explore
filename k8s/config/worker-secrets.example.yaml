# Example only. The local preparation script copies DATABASE_URL from the
# running Compose worker without printing it. Never commit the real Secret.
apiVersion: v1
kind: Secret
metadata:
  name: semantic-layer-secrets-example
  namespace: semantic-layer
type: Opaque
stringData:
  DATABASE_URL: postgres://physicalai:REPLACE_ME@postgres:5432/physicalai
