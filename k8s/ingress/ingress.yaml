apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend
  namespace: semantic-layer
spec:
  rules:
    - host: physicalai.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service: { name: frontend, port: { name: http } }
