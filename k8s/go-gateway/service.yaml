apiVersion: v1
kind: Service
metadata:
  name: go-gateway
  namespace: semantic-layer
spec:
  selector: { app: go-gateway }
  ports:
    - { name: http, port: 8080, targetPort: http }
    - { name: ssh, port: 2222, targetPort: ssh }
